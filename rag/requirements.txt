# Sentinel RAG venv (~/.sentinel/rag/.venv)
# Last verified: 2026-05-17
# Install: python3.14 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Note: huggingface_hub 1.8.0 + httpx 0.28.1 has a HEAD-check bug — set HF_HUB_OFFLINE=1 once the BGE model is cached.

sentence-transformers==5.3.0
chromadb==1.5.5
rank-bm25==0.2.2
certifi==2026.2.25
transformers==5.4.0
httpx==0.28.1
numpy==2.4.4
tqdm==4.67.3
